// Comment

grade1 := 5;
grade2 := 3;

fn add(a: int, b: int) -> int {
    a + b
}

/*
    Multiline
    Comment
*/

result := if add(grade1, grade2) / 2 > 3 { "great" } else { "not really" };
println(result);

age: int;
age = 10;

fn whatever() -> int {
    a := 11;
    b := 22;

    if b > a {
        return b / a;
    }

    b * a
}
